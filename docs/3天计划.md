# 11 天冲刺计划 — 初赛功能完整度（截止 7.20）

## 分工

| 角色 | 负责 |
|------|------|
| 你（组长） | Agent（Go）+ 麒麟部署 + PPT |
| 队员 A | 后端（Go API + 数据库） |
| 队员 B | 前端（React 全部页面） |

---

## 第一阶段：铺页面 + 补后端（Day 1-3）

### 队员 B（前端）

- Day 1：日志库页 `/log-stores` + 日志查询页 `/logs`（壳子，假数据能看就行）
- Day 2：备份管理页 `/backup` + 数据分析页 `/analysis`（趋势图+柱状图）
- Day 3：Agent 详情页 `/agent/:id`（多 Tab）+ 用户管理页 `/users` + 侧边栏菜单更新

### 队员 A（后端）

- Day 1：加 `log_stores` / `logs` / `backups` 三张表 + 对应 CRUD API
- Day 2：`sys_user` 表 + 用户 CRUD API + 操作日志 API + 数据分析统计 API
- Day 3：仪表盘 API 扩展 + 所有新 API 文档更新

### 你（组长 + Agent）

- Day 1：Agent 加日志采集（读 `/var/log/messages` → 推后端）
- Day 2：攻击模拟脚本完善 + 告警端到端验证
- Day 3：systemd service + 一键部署脚本 + 部署文档

---

## 第二阶段：接数据 + 联调（Day 4-6）

### 队员 B
- Day 4：日志模块接真实 API
- Day 5：Agent 详情页接真实数据（监控曲线、操作历史）
- Day 6：所有页面空态/加载态/错误态补全

### 队员 A
- Day 4：配合前端联调，接口字段对齐
- Day 5：权限管理完善 + 操作日志存储完善
- Day 6：bug 修复，性能优化

### 你
- Day 4：日志采集端到端验证 + 造测试数据
- Day 5：攻击 → 告警 → AI 分析 链路的演示彩排
- Day 6：Agent 稳定性测试（30 分钟不 crash）

---

## 第三阶段：PPT + 预演（Day 7-9）

### 你
- Day 7：PPT 初稿（架构图 + 模块截图 + 技术亮点）
- Day 8：PPT 终稿 + 演示脚本
- Day 9：全流程彩排，计时

### 队员 A
- Day 7：配合联调，补数据
- Day 8：后端代码整理，注释清理
- Day 9：协助彩排

### 队员 B
- Day 7：UI 统一走查（色彩 #151B29 + #1890FF）
- Day 8：各页面高清截图
- Day 9：最后修 bug

---

## 第四阶段：收尾（Day 10-11）

- Day 10：录演示视频（5-8 分钟）
- Day 11：最终验收 + 材料检查

---

## 你要做的具体事项（Day 1 开始）

**1. Agent 加日志采集**
文件：`cmd/agent/main.go`
- 新增 `collectLogs()` 函数
- 读 `/var/log/messages`（麒麟系统日志）最后 100 行
- 每 60 秒推一次：`POST /api/logs/push`
- 格式：`{"agent_id":"xxx","store":"系统日志","lines":["...","..."]}`

**2. 攻击模拟脚本**
文件：`scripts/simulate_attack.sh`
- CPU 打高（死循环或 stress）
- SSH 暴力破解模拟
- 确认前端告警页出现新告警

**3. systemd service**
文件：`deploy/safe-agent.service`
- Agent 开机自启
- 写部署文档

---

## 队员 A 要做的（后端）

新增 API：

| API | 用途 |
|-----|------|
| `GET /api/log-stores` | 日志库列表 |
| `POST /api/logs/push` | Agent 推送日志 |
| `GET /api/logs` | 日志查询 |
| `GET /api/backups` | 备份列表 |
| `GET/POST/PUT/DELETE /api/users` | 用户 CRUD |
| `GET /api/operate-logs` | 操作日志 |
| `GET /api/analysis/trend` | 日志趋势统计 |

新增表：`log_stores`、`logs`、`backups`、`sys_user`、`operate_logs`

---

## 队员 B 要做的（前端）

新增页面：

| 页面 | 路径 |
|------|------|
| 日志库 | `/log-stores` |
| 日志查询 | `/logs` |
| 备份管理 | `/backup` |
| 数据分析 | `/analysis` |
| Agent 详情 | `/agent/:id` |
| 用户管理 | `/users` |

完善页面：仪表盘图表、AI 对话历史会话、侧边栏菜单

---

## 评分重点

1. **页面数量** — LCA 有的尽量都有
2. **演示流畅** — 不卡顿、不报错
3. **核心链路** — 日志采集 → 告警 → AI 分析 → 执行修复
4. **技术亮点** — SSE 流式 AI、Agent 心跳架构
